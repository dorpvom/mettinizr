dist: xenial
language: python
python: 3.5
before_install:
  - sudo apt-get update
  - sudo apt-get install libpython3-dev
install:
  - pip install -r requirements.txt
  - ./dependencies.sh
script:
  - pytest --cov 
after_success:
  - coveralls
